# 仓储大屏与系统前端

该目录为 Vue3 + Vite 雏形项目，包含：
- 库的大屏（全屏展示 + 自动轮播）
- 系统界面（首页、列表、详情、配置）

## 运行
```bash
npm install
npm run dev
```

## Mock 数据
- 数据定义：`src/mock/data.js`
- API 占位：`src/api/warehouse.js`

后续对接真实后端时，仅需替换 `src/api` 中的请求实现。
